#!/usr/bin/env python3
import os
import sys

# Here we could set up an automatic call to the sync_incremental MCP tool
# Or we can just advise Claude Code via instructions. We already have the tool registered.
