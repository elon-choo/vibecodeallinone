# SESSION_25_PROMPT

프로젝트: `/Users/elon/Documents/elon_opensource/claude-code-power-pack/`

이번 세션은 `S25`이며, 목적은 **opt-in KG memory broker backend/contracts foundation을 additive-only path로 landing하는 것**이다.

## Activation Note

- 이 prompt는 `S24`에서 live operator validation foundation이 productized path와 blocker artifact까지 landing된 뒤 공식 `SESSION_25` prompt로 승격된 버전이다.
- 이번 세션은 canonical handover / next prompt / root mirror를 갱신하는 official chain session이다.

## Session Size Gate

- 이번 세션은 `memory broker backend/contracts foundation`만 다룬다.
- web control-plane alignment, browser smoke, managed quickstart 확장, reminder follow-up policy는 다음 세션으로 넘긴다.

## 반드시 먼저 읽을 문서

1. `PROJECT_AUDIT.md`
2. `MASTER_PLAN.md`
3. `SESSION_OPERATIONS.md`
4. `HANDOVER.md`
5. `docs/session-ops/01_SESSION_BOARD.md`
6. `docs/session-ops/13_PRODUCT_REPLAN_MASTER.md`
7. `docs/session-ops/14_INSTALL_TELEGRAM_MEMORY_ARCHITECTURE.md`
8. `docs/session-ops/15_EXECUTION_WAVES.md`
9. `docs/session-ops/18_POST_S21_LATER_WAVE_ORCHESTRATION_PLAN.md`
10. `docs/session-ops/handovers/SESSION_24_HANDOVER.md`
11. `kg-mcp-server/API.md`
12. `packages/contracts/openapi/assistant-api.openapi.yaml`
13. `services/assistant-api/assistant_api/models.py`
14. `services/assistant-api/assistant_api/store.py`
15. `services/assistant-api/assistant_api/app.py`

## 이번 세션의 핵심 미션

1. workspace/project memory broker foundation을 backend에 추가한다.
2. opt-in / workspace-scoped / additive-only contract를 정리한다.
3. KG dependency가 always-on hidden path가 되지 않게 막는다.
4. targeted backend tests와 docs/handover를 남긴다.

## 강한 제약

1. explicit user memory와 continuity memory를 깨지 마라.
2. KG를 모든 request의 mandatory dependency로 만들지 마라.
3. broad web UI나 Telegram full memory surface로 세션을 넓히지 마라.
4. managed quickstart/live validation scope를 다시 열지 마라.

## 기대 산출물

1. additive memory broker module/foundation
2. backend contract/model/store/app path
3. targeted tests
4. `SESSION_25_HANDOVER.md`와 `SESSION_26` next prompt

## 종료 조건

1. backend가 workspace/project memory opt-in state를 표현할 수 있다.
2. broker path가 workspace-scoped이고 additive-only다.
3. no always-on KG dependency가 유지된다.
4. validation이 통과한다.

## 필수 validation

1. `.agent-orchestrator/config.json`의 validation 명령
2. 이번 세션에서 추가/갱신한 memory-broker backend targeted checks
